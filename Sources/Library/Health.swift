extension EntityHealth {
    
    static var low: EntityHealth {
        let entityHealth = EntityHealth(value: 10)
        return entityHealth
    }
    
    static var medium: EntityHealth {
        let entityHealth = EntityHealth(value: 50)
        return entityHealth
    }
    
    static var high: EntityHealth {
        let entityHealth = EntityHealth(value: 100)
        return entityHealth
    }
    
}
